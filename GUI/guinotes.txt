game handler class creates ui instance objects
ui instance objects must contain three methods
    init that sets up data
    draw that draws to screen
    check click that returns the name of button pressed

button class
    data
        rect (x, y, w, h)
        font size
        font spacing
        color
        text color

    init

    draw
        draw rectangle
        draw text
        maybe draw texture later?